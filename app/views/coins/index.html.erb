<p style="color: green"><%= notice %></p>

<h1>Moedas</h1>

<p><%= data_br Date.today %></p>

<table>
  <thead>
    <tr>
      <th><%= Coin.human_attribute_name(:description) %></th>
      <th><%= Coin.human_attribute_name(:acronym) %></th>
      <th><%= Coin.human_attribute_name(:url_image) %></th>
      <th colspan="1"></th>
    </tr>
  </thead>
  <tbody>
    <% @coins.each do |coin| %>
      <tr>
        <td><%= coin.description %></td>
        <td><%= coin.acronym %></td>
        <td><%= image_tag coin.url_image, width:25, height:25 %></td>
        <td><%= link_to t('links.show'), coin %></td>
        <td><%= link_to t('links.edit'), edit_coin_path(coin)%></td>
        <td><%= button_to t('links.destroy'), coin,method: :delete, form: { data: { turbo_confirm: t('you_sure') } } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Nova moeda", new_coin_path %>
